// Начало программы
begin
  write(1); // печатаем на экран 1
  write(2); // печатаем на экран 2

  // /* это не считается вложенным комментарием */
  /* // ровно, как и это */
  /*
    // или
    // это
  */

  skip;
  skip;
  skip;
  skip;
  skip;
  // печатаем на экран 3:
  write(3);
  skip
end  /* конец программы */

// Тестируем, что у нас всё не крашнется, если коммент в конце:
//